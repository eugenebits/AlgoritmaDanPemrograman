/* Import font yang manis */
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap');

body {
  font-family: 'Poppins', sans-serif;
  background: linear-gradient(135deg, #ffe6f0, #e6ccff);
  color: #4a2c4d;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  margin: 0;
}

/* Container utama */
.container {
  background: #fff0f6;
  border-radius: 20px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.1);
  padding: 30px;
  width: 90%;
  max-width: 450px;
  text-align: center;
  animation: fadeIn 0.8s ease;
}

h1 {
  font-size: 1.8rem;
  margin-bottom: 20px;
  color: #b30059;
}

/* Input */
input {
  width: 80%;
  padding: 10px;
  border: 2px solid #ffb3d9;
  border-radius: 15px;
  outline: none;
  margin-bottom: 15px;
  font-size: 1rem;
  transition: all 0.3s ease;
}

input:focus {
  border-color: #cc66ff;
  box-shadow: 0 0 8px #cc66ff;
}

/* Button */
button {
  background: #ff80bf;
  color: white;
  border: none;
  border-radius: 15px;
  padding: 10px 15px;
  margin: 5px;
  cursor: pointer;
  font-size: 1rem;
  transition: all 0.3s ease;
}

button:hover {
  background: #cc66ff;
  transform: scale(1.05);
}

/* Judul kecil */
h3 {
  margin-top: 25px;
  color: #732673;
}

/* Box untuk daftar & status */
#daftarAntrian,
#status {
  background: #fff;
  border-radius: 12px;
  padding: 15px;
  margin-top: 10px;
  min-height: 40px;
  font-size: 0.95rem;
  color: #4a2c4d;
  border: 1px solid #ffccf2;
  transition: all 0.4s ease;
}

/* Item antrian (kalau mau extra styling selain JS) */
.antrian-item {
  background: #ffe6f7;
  margin: 6px 0;
  padding: 8px 12px;
  border-radius: 12px;
  border: 1px solid #ffb3d9;
  font-weight: 500;
  transition: transform 0.3s ease, background 0.3s ease;
}

.antrian-item:hover {
  background: #ffccf2;
  transform: scale(1.03);
}

/* Animasi masuk container */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(15px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
